name: learn-github-actions
on: [push]
jobs:
#  check-bats-version:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v2
#      - uses: actions/setup-node@v1
#      - run: npm install -g bats
#      - run: bats -v
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Install docker
        run: apt install docker
        shell: bash

  build:
    needs: setup
    steps:
      - name: Build docker image
        run: ./bin/build.sh
        shell: bash
  test:
    needs: build
    steps:
      - name: Run examples
        run: ./examples/OASIS_TOSCA_1.2/run.sh
        run: ./examples/OpenStack/run.sh
    

